SkeletalDefiner
===============

3D tool for defining a skeletal structure


To compile you will need the following (on Debian-based distros: setupenvironment.sh):
make
QT standard libraries + QT OpenGL libraries. (just get qtcreator)
libassimp-dev
